<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <EditTextPreference
        android:title="Username"
        android:key="@string/key_username"
        android:summary="Enter or change your username" />
    <CheckBoxPreference
        android:title="Application Updates"
        android:defaultValue="false"
        android:summary="This option if selected will allow the application to check for latest versions."
        android:key="applicationUpdates" />
    <ListPreference
        android:title="App Theme"
        android:summary="Select a different colour theme for the app. View the app in new eyes!"
        android:key="@string/key_theme"
        android:defaultValue="1"
        android:entries="@array/styleArray"
        android:entryValues="@array/styleValues" />
</PreferenceScreen>